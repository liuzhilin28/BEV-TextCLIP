# ============================================================
# BEV-TextCLIP 依赖清单 (GPU版本)
# 适用于 Linux 服务器 + NVIDIA Titan RTX
#
# Author: BY ChangXiu.Huang
# Copyright: ChangXiu.Huang
# Date: 2026-01-25
# ============================================================

# ============================================================
# 安装说明
# ============================================================
# Windows/macOS (CPU):
#   pip install -r requirements.txt
#
# Linux GPU (CUDA 11.8):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#   pip install -r requirements.txt
#
# Docker (推荐):
#   docker build -t bev-textclip:v1.0 -f Dockerfile .
#   ./start.sh build

# ============================================================
# PyTorch GPU 版本 (CUDA 11.8)
# 注意：在Docker中会自动安装，无需手动安装
# ============================================================
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0

# ============ 训练框架 ============
pytorch-lightning==2.6.0
torchmetrics==1.8.2

# ============ CLIP ============
# clip-by-openai 是官方 CLIP Python 包
clip-by-openai
transformers>=4.30.0
sentence-transformers>=2.2.0

# ============ 数据处理 ============
numpy==1.26.2
pandas==2.1.4
scikit-learn==1.7.1
scipy>=1.10.0
h5py==3.14.0
plyfile==1.1.3
open3d==0.19.0

# ============ 可视化 ============
matplotlib==3.10.5
seaborn==0.13.2
tensorboard==2.20.0
plotly==6.0.0

# ============ 配置 ============
pyyaml==6.0.1
omegaconf==2.3.0

# ============ 工具 ============
tqdm==4.65.2
requests==2.31.0
urllib3==2.2.1
colorlog==6.8.0

# ============ API 服务 ============
fastapi==0.115.5
uvicorn==0.32.0
pydantic==2.10.1
python-multipart==0.0.20

# ============ 模型导出 ============
onnx==1.17.0
onnxruntime==1.20.0
onnxruntime-gpu==1.20.0

# ============ 分布式训练 ============
deepspeed==0.16.0
accelerate>=0.20.0

# ============ 日志和监控 ============
wandb==0.19.1
mlflow==2.17.2

# ============ nuScenes 数据集支持 ============
nuscenes-devkit>=1.1.0

# ============ 点云处理 ============
open3d==0.19.0
laspy==2.5.4
trimesh==4.2.4

# ============ 开发工具 ============
pytest==8.3.3
pytest-cov==6.0.0
black==24.10.0
isort==5.13.2
flake8==7.1.1
mypy==1.14.0
